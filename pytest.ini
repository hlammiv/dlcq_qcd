[pytest]
testpaths = tests
markers =
    slow: expensive tests (Richardson sweeps, large K); excluded by default
    fortran: needs Fortran output files or the compiled binary
addopts = -q --strict-markers
